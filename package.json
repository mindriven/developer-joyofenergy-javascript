{
  "name": "joi-energy-js",
  "version": "1.0.0",
  "description": "",
  "type": "commonjs",
  "scripts": {
    "test": "jest --coverage",
    "cucumber": "cucumber-js",
    "test-watch": "jest --watchAll",
    "start": "node src/app",
    "dev": "nodemon --inspect",
    "build": "flow-remove-types ./flowtyped_src -d src/ -x .json,.js",
    "flow": "flow check ./flowtyped_src",
    "hot-build": "nodemon -e js --watch flowtyped_src --exec \"npm run build && npm run start\""
  },
  "author": "Gabriel McCallin",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "@cucumber/cucumber": "^8.5.1",
    "@types/jest": "^26.0.19",
    "flow-bin": "^0.184.0",
    "flow-remove-types": "^2.184.0",
    "jest": "^26.6.3",
    "nodemon": "^2.0.6"
  }
}
